[package]
name = "sol-analytics"
version = "0.1.0"
edition = "2021"
description = "Browser-side series analytics for sol-analyzer (compiled to WebAssembly)"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "=0.2.100"

[profile.release]
opt-level = "s"
lto = true
